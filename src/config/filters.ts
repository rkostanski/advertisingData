import { IFilters } from '../interfaces/IFilters';

export const filters: IFilters = {
  datasource: {
    label: 'Datasource',
    data: new Set(),
  },
  campaign: {
    label: 'Campaign',
    data: new Set(),
  },
};
